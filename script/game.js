function createGame () {
  let containerVisible = document.getElementById('game-container');
  let ballEnergy = document.getElementById('energy');
  
  containerVisible.classList.add('is-visible');
  ballEnergy.classList.add('is-visible');
}